[Sender selects file] → metadata only
      |
[Create Room → Peers join]
      |
[Sender clicks "Send File" → chunks streamed via WebRTC]
      |
[Receiver collects chunks → assembles → download]
      |
[Optional: progress bar + resume if interrupted]

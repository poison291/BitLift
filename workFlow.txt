[Sender selects file] → only get file metadata
      |
[Create Room → Peers join]
      |
[Sender clicks "Stream File" → chunks streamed via WebRTC]
      |
[Receiver collects chunks → assembles → download]

